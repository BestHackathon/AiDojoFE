import { CutTillCommaPipe } from './cut-till-comma.pipe';

describe('CutTillCommaPipe', () => {
  it('create an instance', () => {
    const pipe = new CutTillCommaPipe();
    expect(pipe).toBeTruthy();
  });
});
